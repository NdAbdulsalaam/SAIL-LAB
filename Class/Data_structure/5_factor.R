# Factor 
# Factors are vectors but can store only predefined values. Mostly used for storing categorical data. They have two attributes (class & levels).
# Class = “factor”

# Levels = Set of allowed values

# Factors can be created using the factor().

fac <- factor(c("a", "b", "c", "b", "c"))
fac
